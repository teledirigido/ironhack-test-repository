# ironhack-test-repository

This is my `first` repository.
- item 1
- item 2

### Initialize an empty GIT repo
You have to do this command:  
```
git init
```

### My `git add` commands:

1. To add all files: 
```git add -A```
2. To add files from current folder/location: 
```git add .```
3. To add a particular file
```git add [filename]```